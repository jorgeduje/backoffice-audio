import { Typography } from "@mui/material";

const Home = () => {
  return (
    <div>
      <Typography variant="h1" color={"primary"}>
        Home
      </Typography>
    </div>
  );
};

export default Home;
